Masterclass: MLOps - Gestión de Datos

/\* Estilos adicionales para animaciones suaves \*/
.details-content {
transition: all 0.3s ease-in-out;
overflow: hidden;
}
.details-content.hidden {
display: none;
opacity: 0;
max-height: 0;
}
.details-content.visible {
opacity: 1;
max-height: 500px;
/\* Suficiente para el contenido \*/
}
.topic-card {
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Fira+Code:wght@400;500&display=swap');
body {
font-family: 'Inter', sans-serif;
background-color: #f8fafc;
color: #0f172a;
line-height: 1.6;
}
.code-block {
font-family: 'Fira Code', monospace;
background-color: #0f172a;
color: #e2e8f0;
padding: 1.25rem;
border-radius: 0.75rem;
font-size: 0.85rem;
line-height: 1.6;
border-left: 4px solid #3b82f6;
overflow-x: auto;
margin-bottom: 1.5rem;
}
.py-comment {
color: #64748b;
font-style: italic;
}
.py-keyword {
color: #f472b6;
}
.py-func {
color: #38bdf8;
}
.py-str {
color: #fbbf24;
}
.phase-indicator {
@apply inline-block px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest mb-4 bg-blue-600 text-white shadow-sm;
}
.sub-stage {
@apply border-l-2 border-slate-200 pl-6 mb-12 relative;
}
.sub-stage::before {
content: '';
@apply absolute -left-[9px] top-0 w-4 h-4 bg-blue-600 rounded-full border-4 border-white shadow-sm;
}
.alert-card {
@apply bg-rose-50 border-l-4 border-rose-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.best-practice {
@apply bg-blue-50 border-l-4 border-blue-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.health-badge {
@apply px-2 py-0.5 rounded text-[10px] font-bold border flex items-center gap-1 bg-white;
}

# Data Management

MLOps Pillar #1: Datos como
Código

### Definición

Es la práctica de ingeniería de aplicar principios de DevOps al manejo de datos (DataOps). En
MLOps, implica garantizar que los datasets sean \*\*inmutables, versionados y validados\*\*,
eliminando la discrepancia entre el entrenamiento y la inferencia en tiempo real.

#### Filosofía Data-Centric AI

"El rendimiento de la IA mejora más iterando sobre la **calidad de los datos** que
ajustando el algoritmo. Si el dato es ruidoso, el modelo será ruidoso, sin importar su
complejidad."

## Conceptos Clave

- **Consistencia Online/Offline:** Garantizar que el modelo
  use las mismas transformaciones en entrenamiento que en producción.
- **Feature Store:** Almacén centralizado que sirve como
  "fuente de verdad" para las variables predictivas.
- **Data Lineage:** Trazabilidad total del origen del dato y
  cada transformación que sufrió hasta llegar al modelo.

## Análisis de Valor

#### Ventajas

- • Reproducibilidad total de experimentos históricos.
- • Detección temprana de anomalías en el flujo de entrada.
- • Cumplimiento normativo (GDPR/PII) simplificado.

#### Desventajas

- • Alta latencia inicial por procesos de validación.
- • Costo elevado de almacenamiento de versiones (Snapshots).
- • Curva de aprendizaje técnica (DVC, Feature Stores).

### Ingesta y Conectividad

Extracción de fuentes heterogéneas

- Conectores para SQL, NoSQL y Data Lakes
- Ingesta en tiempo real (Streaming) vs Batch
- Estrategias de captura de datos cambiantes (CDC)
- Desacoplamiento de fuentes mediante Message Brokers

Herramientas Comunes:

Apache
Kafka
AWS
Glue
Airbyte
Fivetran

### Pipelines de Ingeniería (ETL/ELT)

Transformación y limpieza a escala

- Normalización y limpieza de valores nulos
- Codificación de variables categóricas
- Escalado y transformación de características
- Orquestación de flujos de trabajo

Herramientas Comunes:

Apache
Spark
dbt
Airflow
Prefect

### Feature Store

El repositorio central de características

- Reutilización de features entre equipos
- Consistencia entre entrenamiento y servicio
- Servicio de baja latencia para predicciones online
- Cálculo automático de características

Herramientas Comunes:

Feast
Tecton
Hopsworks
Databricks

### Versionado de Datos (DVC)

Git para tus datasets

- Seguimiento de versiones de grandes volúmenes de datos
- Vinculación de datos con versiones de código
- Reproducibilidad total de experimentos
- Gestión de almacenamiento en la nube (S3, GCS)

Herramientas Comunes:

DVC
Pachyderm
LakeFS
Git
LFS

### Validación y Calidad

Garantizando la integridad

- Validación de esquemas y tipos de datos
- Detección de valores atípicos (Outliers)
- Pruebas de unidad para datasets
- Monitoreo de estadísticas de distribución

Herramientas Comunes:

Great
Expectations
Deequ
Pandera
TFDV

### Linaje y Gobernanza

Trazabilidad de la procedencia

- Auditoría de origen y transformaciones
- Cumplimiento de normativas (GDPR/PII)
- Catálogo de metadatos
- Control de acceso basado en roles (RBAC)

Herramientas Comunes:

Amundsen
Apache
Atlas
DataHub
Collibra

### El Concepto "Data-Centric"

A diferencia del software tradicional, en ML el rendimiento mejora más iterando sobre la
**calidad de los datos** que ajustando infinitamente el
algoritmo.

01

#### Consistencia Online/Offline

Evita que el modelo vea datos distintos en
entrenamiento vs. realidad.

02

#### Reproducibilidad

Poder recrear un modelo exacto de hace 6 meses
usando el "snapshot" de datos.

#### Riesgo: Entrenamiento-Servicio (Skew)

Ocurre cuando las transformaciones de datos en producción no coinciden exactamente con las
de entrenamiento. Una Feature Store es la solución técnica estándar para este problema.

## Etapas del Ciclo de Vida de Datos

1. Ingesta y Conectividad

### Extracción Multifuente

Desacoplamiento de fuentes mediante brokers de mensajes o
conectores inmutables.

# Ejemplo: Ingesta determinista desde un Feature Store (Feast)
from feast import FeatureStore
store = FeatureStore(repo\_path=".")
training\_df = store.get\_historical\_features(
entity\_df=entity\_df,
features=["driver\_stats:acc\_rate", "driver\_stats:rating"]
).to\_df()

2. Pipelines de Ingeniería (ETL)

### Limpieza y Transformación a Escala

Uso de orquestadores para automatizar la normalización y
escalado.

# Transformación modular con dbt o scripts integrados
def preprocess\_features(df):
# Tratamos nulos de forma reproducible
df['age'] = df['age'].fillna(df['age'].median())
# Codificación categórica
return pd.get\_dummies(df,
columns=['geo'])

3. Validación y Calidad

### Unit Tests para Datasets

El modelo no debe entrenar si el dato no cumple el contrato de
esquema.

import great\_expectations as ge
# Definir expectativas de calidad
df\_ge = ge.from\_pandas(training\_df)
df\_ge.expect\_column\_values\_to\_not\_be\_null("driver\_id")
df\_ge.expect\_column\_values\_to\_be\_between("rating", 0, 5)

4. Versionado de Datos (DVC)

### Git para Big Data

Vinculamos el código con el hash inmutable de los datos.

# Comando CLI simulado en bloque descriptivo
$ dvc add data/raw\_dataset.csv
$ git add data/raw\_dataset.csv.dvc .gitignore
$ git commit -m "Versionado dataset v1.4 para experimento #42"
$ dvc push # Envía el dataset real a S3/GCS

#### Error Fatal: Training-Serving Skew

Ocurre cuando la lógica de cálculo de una feature en el notebook de entrenamiento (Pandas) difiere
de la lógica en la API de producción (SQL/Java). \*\*Solución:\*\* Implementar una Feature Store que
centralice el cálculo de la feature para ambos entornos.

## Health Check: Fase de Datos

Integridad

¿El 100% de las columnas cumplen con el tipo de
dato esperado?

Frescura

¿La latencia de los datos en tiempo real es menor
a 500ms?

Deriva (Drift)

¿La distribución estadística de las features ha
cambiado > 10%?

## Checklist MLOps: Gestión de Datos

##### Inmutabilidad Garantizada

¿Se ha prohibido la sobreescritura de archivos de datos existentes?

##### Esquema Validado

¿El pipeline de datos falla si se detecta una columna nueva o
faltante?

##### Snapshotting de Experimento

¿Cada corrida de MLflow está vinculada a un hash de datos específico?

##### Anonimización (PII)

¿Se han eliminado o cifrado datos sensibles antes de entrar al
Feature Store?

##### Monitoreo de Nulls

¿Existe una alerta automática si el porcentaje de nulos sube del
histórico?

##### Lineage End-to-End

¿Podemos rastrear el dato desde el Dashboard final hasta la DB de
origen?

Industrial Standard v13.0 • MLOps Framework • 2024

document.addEventListener('DOMContentLoaded', () => {
// Inicializar iconos
lucide.createIcons();
const cards = document.querySelectorAll('.topic-card');
cards.forEach(card => {
card.addEventListener('click', () => {
const index = card.dataset.index;
const color = card.dataset.color;
// Resetear todas las tarjetas
cards.forEach(c => {
// Clases inactivas
c.classList.remove('bg-white', 'shadow-xl', 'border-' + c.dataset.color + '-500', 'translate-x-2', 'active');
c.classList.add('bg-slate-50', 'border-slate-200');
// Ocultar detalles
const details = c.querySelector('.details-content');
details.classList.remove('visible');
details.classList.add('hidden');
});
// Activar tarjeta clickeada
card.classList.remove('bg-slate-50', 'border-slate-200');
card.classList.add('bg-white', 'shadow-xl', 'border-' + color + '-500', 'translate-x-2', 'active');
// Mostrar detalles
const details = card.querySelector('.details-content');
details.classList.remove('hidden');
details.classList.add('visible');
});
});
});