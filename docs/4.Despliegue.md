Masterclass: Despliegue y Serving en MLOps

/\* Estilos para animaciones suaves \*/
.details-content {
transition: all 0.3s ease-in-out;
overflow: hidden;
}
.details-content.hidden {
display: none;
opacity: 0;
max-height: 0;
}
.details-content.visible {
opacity: 1;
max-height: 600px;
}
.topic-card {
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
@keyframes bounce {
0%,
100% {
transform: translateY(0);
}
50% {
transform: translateY(-10px);
}
}
.animate-bounce-custom {
animation: bounce 2s infinite;
}

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Fira+Code:wght@400;500&display=swap');
body {
font-family: 'Inter', sans-serif;
background-color: #f8fafc;
color: #0f172a;
line-height: 1.6;
}
.code-block {
font-family: 'Fira Code', monospace;
background-color: #0f172a;
color: #e2e8f0;
padding: 1.25rem;
border-radius: 0.75rem;
font-size: 0.85rem;
line-height: 1.6;
border-left: 4px solid #f97316;
overflow-x: auto;
margin-bottom: 1.5rem;
}
.py-comment {
color: #64748b;
font-style: italic;
}
.py-keyword {
color: #f472b6;
}
.py-func {
color: #38bdf8;
}
.py-str {
color: #fbbf24;
}
.phase-indicator {
@apply inline-block px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest mb-4 bg-orange-600 text-white shadow-sm;
}
.sub-stage {
@apply border-l-2 border-slate-200 pl-6 mb-12 relative;
}
.sub-stage::before {
content: '';
@apply absolute -left-[9px] top-0 w-4 h-4 bg-orange-600 rounded-full border-4 border-white shadow-sm;
}
.alert-card {
@apply bg-rose-50 border-l-4 border-rose-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.best-practice {
@apply bg-orange-50 border-l-4 border-orange-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.health-badge {
@apply px-2 py-0.5 rounded text-[10px] font-bold border flex items-center gap-1 bg-white;
}

# Serving Industrial

MLOps Pillar #4: El Momento de
la Verdad

### Definición

Es la fase donde el artefacto del modelo se transforma en un servicio productivo. No solo
implica "exponer una API", sino garantizar **alta disponibilidad, baja latencia y
escalabilidad elástica** frente a la demanda fluctuante del negocio.

#### Elemento Distintivo

"Un modelo en Serving es software vivo. A diferencia de una app tradicional, el Serving debe
gestionar el **vínculo inquebrantable** entre la versión del código del servidor y
la versión de los pesos del modelo."

## Conceptos Pilares

- **Inferencia de Baja Latencia:** Optimización técnica para
  que el modelo responda en milisegundos (mínimo P99).
- **Infraestructura Inmutable:** Uso de contenedores para
  asegurar que el entorno de predicción sea idéntico al de entrenamiento.
- **Traffic Splitting:** Capacidad de redirigir porcentajes de
  usuarios a diferentes versiones del modelo sin caída del servicio.

## Análisis de Valor

#### Ventajas

- • Escalabilidad automática ante picos de demanda.
- • Despliegue de modelos sin riesgo (Zero-Downtime).
- • Optimización de costos mediante "Scale to Zero".

#### Desventajas

- • Alta complejidad en orquestación (K8s/Istio).
- • Latencia introducida por capas de red y contenedores.
- • Costo operativo de mantener clústeres activos 24/7.

### Servicio en Tiempo Real (Online)

Baja latencia y alta disponibilidad

- Inferencia mediante APIs REST o gRPC
- Gestión de colas para picos de tráfico
- Caché de predicciones comunes
- Soporte para microservicios independientes

Infraestructura Sugerida:

FastAPI
KServe
Seldon
NVIDIA
Triton

### Procesamiento por Lotes (Batch)

Escalabilidad para grandes volúmenes

- Inferencia masiva programada (ej. cada 24h)
- Escritura directa en bases de datos o Data Lakes
- Optimización de costos mediante Spark/Ray
- Ideal para recomendaciones o scoring de riesgo

Infraestructura Sugerida:

Apache
Spark
Airflow
Metaflow
Ray

### Inferencia en el Borde (Edge)

Privacidad y respuesta instantánea

- Ejecución en dispositivos móviles o IoT
- Reducción de dependencia de la nube
- Menor latencia de red y mayor privacidad
- Adaptación a hardware limitado

Infraestructura Sugerida:

TensorFlow
Lite
CoreML
ONNX
Runtime
TinyML

### Optimización de Modelos

Haciendo los modelos más ligeros

- Cuantización (de FP32 a INT8)
- Poda (Pruning) de pesos innecesarios
- Destilación de conocimiento (Student/Teacher)
- Compilación para hardware específico (GPU/TPU)

Infraestructura Sugerida:

OpenVINO
TensorRT
TVM
Neural
Magic

### Escalado y Orquestación

Infraestructura elástica

- Autoscaling basado en CPU/GPU o tráfico
- Gestión de clústeres elásticos (Kubernetes)
- Servicio sin servidor (Serverless Inference)
- Optimización de costos en la nube

Infraestructura Sugerida:

Kubernetes
Knative
AWS
Lambda
GCP
Vertex

### Seguridad y Control de Acceso

Protegiendo el punto de entrada

- Autenticación JWT y API Keys
- Limitación de tasa (Rate Limiting)
- Aislamiento de contenedores
- Cifrado de datos en tránsito y reposo

Infraestructura Sugerida:

Kong
Istio
Auth0
HashiCorp
Vault

### Métricas Críticas

Para evaluar el serving, no miramos la precisión (accuracy), sino la eficiencia del sistema.

Latencia (P99)
< 100ms

Throughput
Req/seg

Costo por Inferencia
$ / 1k pred

#### Inferencia Serverless

Ideal para modelos con tráfico esporádico. Escala a cero cuando no se usa, ahorrando costos
significativos, aunque puede sufrir del problema de "Cold Start".

## Pipeline de Inferencia Industrial

1. Inferencia en Tiempo Real (FastAPI)

### Definición del Endpoint

El modelo se expone mediante una interfaz RESTful que valida los
datos de entrada (Pydantic).

from fastapi import FastAPI
from pydantic import BaseModel
import joblib
app = FastAPI()
model = joblib.load("model\_v4.pkl")
class InputData(BaseModel):
feature\_1: float
feature\_2: float
@app.post("/predict")
def predict(data: InputData):
prediction = model.predict([[data.feature\_1, data.feature\_2]])
return {"prediction": float(prediction[0])}

2. Orquestación (Kubernetes)

### Manifiesto de Despliegue y HPA

Configuramos el **Escalado Automático** basado en
CPU para soportar carga masiva.

# deployment.yaml (Snippet conceptual)
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
name: ml-model-hpa
spec:
maxReplicas: 10
metrics:
- type: Resource
resource:
name: cpu
target:
type: Utilization
averageUtilization: 70

3. Canary & A/B Testing

### Estrategias de Reducción de Riesgo

Dividimos el tráfico para validar el nuevo modelo (Challenger)
contra el actual (Champion).

#### Canary Deployment

Lanzar la V2 al 5% de usuarios. Si las métricas de salud
técnica son estables, escalar al 100%.

#### A/B Testing

Comparar dos algoritmos distintos en producción para ver
cuál genera mejor conversión de negocio.

## Gatekeeper: Validación Cruzada (K-Fold)

### Definición Industrial

La Validación Cruzada divide los datos en **K particiones**. El modelo se
entrena y valida K veces, asegurando que su capacidad predictiva sea consistente en todo
el espectro de datos y no dependa de un "golpe de suerte" aleatorio.

Por qué en Serving: Es el paso final del
pipeline automatizado (CT) antes de que el CI/CD autorice el despliegue de un
contenedor.

### Pros & Contras

#### Ventajas

Estimación de error imparcial. Máximo
aprovechamiento de datos limitados. Robustez garantizada.

#### Desventajas

Coste computacional lineal ($O(K)$).
Complejidad en series temporales (requiere orden cronológico).

## Higiene de la Infraestructura de Serving

#### Error: Cold Starts en Serverless

Desplegar modelos pesados (ej. Deep Learning) en funciones Lambda/Cloud Run.
**Impacto:** Los primeros usuarios esperan 30 segundos mientras el contenedor carga
los pesos. **Solución:** Usar "Warm Instances" o despliegues persistentes en K8s.

#### Práctica: Liveness & Readiness Probes

Configurar Kubernetes para que no envíe tráfico al modelo hasta que los pesos estén 100%
cargados en memoria RAM.

### Health Check: Model Serving

##### Latencia P99

< 150ms

##### Success Rate

> 99.9%

##### Cost Efficiency

Optimizado

## Checklist MLOps: Serving y Despliegue

##### Carga Progresiva (Canary) Configurada

¿Tenemos la capacidad de revertir automáticamente si el error rate
sube al 1%?

##### Esquema de API Versionado

¿El endpoint maneja versiones (ej. /v1/predict) para evitar romper
apps clientes?

##### Cuantización / Optimización de Grafo

¿Se ha reducido el peso del modelo (ONNX/TFLite) para optimizar la
RAM del servidor?

##### Validación de Esquema en Inferencia

¿La API rechaza peticiones con datos nulos o tipos incorrectos antes
de tocar el modelo?

##### Seguridad y Rate Limiting

¿Se han implementado API Keys y cuotas para evitar ataques de
denegación de servicio?

##### Linaje de Inferencia (Logs)

¿Se guardan las predicciones (request/response) para el futuro
análisis de Data Drift?

Industrial Standard v17.0 • MLOps Serving Ecosystem • 2024

document.addEventListener('DOMContentLoaded', () => {
// Inicializar iconos
lucide.createIcons();
const cards = document.querySelectorAll('.topic-card');
cards.forEach(card => {
card.addEventListener('click', () => {
const index = card.dataset.index;
const color = card.dataset.color;
// Resetear todas las tarjetas
cards.forEach(c => {
const cColor = c.dataset.color;
// Clases inactivas
c.classList.remove('bg-white', 'shadow-xl', 'border-' + cColor + '-500', 'translate-x-2', 'active');
c.classList.add('bg-slate-50', 'border-slate-200');
// Ocultar detalles
const details = c.querySelector('.details-content');
details.classList.remove('visible');
details.classList.add('hidden');
});
// Activar tarjeta clickeada
card.classList.remove('bg-slate-50', 'border-slate-200');
card.classList.add('bg-white', 'shadow-xl', 'border-' + color + '-500', 'translate-x-2', 'active');
// Mostrar detalles
const details = card.querySelector('.details-content');
details.classList.remove('hidden');
details.classList.add('visible');
});
});
});