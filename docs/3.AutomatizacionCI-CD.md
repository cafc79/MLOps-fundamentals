Masterclass: CI/CD/CT en MLOps

/\* Estilos para animaciones suaves \*/
.details-content {
transition: all 0.3s ease-in-out;
overflow: hidden;
}
.details-content.hidden {
display: none;
opacity: 0;
max-height: 0;
}
.details-content.visible {
opacity: 1;
max-height: 600px;
}
.topic-card {
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Fira+Code:wght@400;500&display=swap');
body {
font-family: 'Inter', sans-serif;
background-color: #f8fafc;
color: #0f172a;
line-height: 1.6;
}
.code-block {
font-family: 'Fira Code', monospace;
background-color: #0f172a;
color: #e2e8f0;
padding: 1.25rem;
border-radius: 0.75rem;
font-size: 0.85rem;
line-height: 1.6;
border-left: 4px solid #6366f1;
overflow-x: auto;
margin-bottom: 1.5rem;
}
.py-comment {
color: #64748b;
font-style: italic;
}
.py-keyword {
color: #f472b6;
}
.py-func {
color: #38bdf8;
}
.py-str {
color: #fbbf24;
}
.phase-indicator {
@apply inline-block px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest mb-4 bg-indigo-600 text-white shadow-sm;
}
.sub-stage {
@apply border-l-2 border-slate-200 pl-6 mb-12 relative;
}
.sub-stage::before {
content: '';
@apply absolute -left-[9px] top-0 w-4 h-4 bg-indigo-600 rounded-full border-4 border-white shadow-sm;
}
.alert-card {
@apply bg-rose-50 border-l-4 border-rose-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.best-practice {
@apply bg-emerald-50 border-l-4 border-emerald-500 p-5 rounded-r-xl mb-8 shadow-sm;
}
.health-badge {
@apply px-2 py-0.5 rounded text-[10px] font-bold border flex items-center gap-1 bg-white;
}

# CI/CD/CT Industrial

MLOps Pillar #3: El Puente a
Producción

### Definición

Es la automatización del ciclo de vida del ML. A diferencia del software tradicional, aquí
integramos no solo **código**, sino también **datos** y
**modelos**. El objetivo es que cualquier cambio en la lógica o en la distribución
de datos dispare automáticamente un flujo de pruebas y despliegue confiable.

#### Elemento Distintivo: CT

"En DevOps el despliegue es el fin. En MLOps, el despliegue es el inicio del
**Entrenamiento Continuo (CT)**: el sistema debe ser capaz de re-entrenarse solo
cuando el mundo real cambia."

## Conceptos Pilares

- **CI (Integración Continua):** Pruebas unitarias de scripts
  y validación de la integridad de los nuevos datos de entrenamiento.
- **CD (Entrega Continua):** Despliegue automático de
  servicios de inferencia (APIs) en entornos de Staging/Prod.
- **Model Registry:** Inventario centralizado donde los
  modelos aprobados se etiquetan y se versionan semánticamente.

## Análisis de Valor

#### Ventajas

- • Cero tiempo de inactividad en actualizaciones.
- • Mitigación de "Training-Serving Skew".
- • Auditoría total de quién puso qué modelo en producción.

#### Desventajas

- • Alta complejidad en orquestación de clústeres.
- • Costo de infraestructura para pipelines paralelos.
- • Riesgo de automatizar el despliegue de sesgos.

### CI: Integración Continua de ML

Pruebas de código, datos y modelos

- Pruebas unitarias para scripts de preprocesamiento
- Validación de calidad de datos en el pipeline
- Pruebas de integridad del modelo (smoke tests)
- Generación automática de artefactos de entrenamiento

Stack
Tecnológico:

GitHub
Actions
GitLab
CI
Pytest
Great
Expectations

### CD: Entrega Continua de Modelos

Automatización del despliegue

- Despliegue automático en entornos de Staging
- Empaquetado en contenedores inmutables
- Promoción de modelos basada en métricas
- Infraestructura como Código (IaC)

Stack
Tecnológico:

Argo
CD
Jenkins
Terraform
Kubernetes

### CT: Entrenamiento Continuo

El corazón de MLOps

- Triggers basados en tiempo o eventos
- Re-entrenamiento automático ante Data Drift
- Pipelines de orquestación extremo a extremo
- Validación comparativa vs. modelo actual (Champion/Challenger)

Stack
Tecnológico:

Kubeflow
Pipelines
TFX
Vertex
AI
Airflow

### Estrategias de Despliegue

Minimizando el riesgo en producción

- Canary Deployments (tráfico progresivo)
- Blue/Green Deployments (cero tiempo de inactividad)
- Shadow Deployments (pruebas en silencio)
- A/B Testing de modelos

Stack
Tecnológico:

Istio
Seldon
Core
KServe
AWS
SageMaker

### Automatización de Contenedores

Entornos reproducibles

- Creación de imágenes Docker optimizadas
- Gestión de dependencias de sistema y GPU
- Seguridad y escaneo de vulnerabilidades
- Orquestación en clústeres elásticos

Stack
Tecnológico:

Docker
Podman
Helm
Google
Artifact Registry

### Niveles de Madurez

#### Nivel 0: Manual

Entrenamiento manual en notebooks. Sin automatización.

#### Nivel 1: CT Automatizado

Pipelines de entrenamiento automáticos ante nuevos
datos.

#### Nivel 2: CI/CD Pleno

Automatización total desde el código hasta el
monitoreo.

#### Diferencia Clave: CT

A diferencia de DevOps, donde el despliegue es el final, en MLOps el **Entrenamiento
Continuo (CT)** asegura que el sistema aprenda de los nuevos datos que llegan a
producción, manteniendo la relevancia del modelo sin intervención humana constante.

"Desplegar código es fácil, desplegar confianza es MLOps."

## Pipeline de Automatización

1. Pruebas Unitarias de Código

### Validación de Lógica de Preprocesamiento

Aseguramos que las funciones de ingeniería de características no
rompan los datos.

# test\_preprocessing.py
import pytest
from src.features import clean\_data
def test\_null\_removal():
raw\_data = pd.DataFrame({'val': [1, None, 3]})
cleaned = clean\_data(raw\_data)
assert cleaned['val'].isnull().sum() == 0
assert len(cleaned) == 3

2. Validación de Modelos

### Gateway de Calidad Predictiva

El modelo solo se registra si supera al modelo actual en
producción (Champion vs Challenger).

# check\_model\_performance.py
def validate\_model(new\_f1, current\_f1,
threshold=0.01):
# Solo permitimos el despliegue si el modelo es al menos 1% mejor
if new\_f1 > (current\_f1 + threshold):
print("Model Approved for Production")
return True
return False

3. Build de Contenedores (Docker)

### Empaquetado del Entorno Inmutable

Garantizamos que el modelo corra exactamente igual en cualquier
nube.

# Dockerfile
FROM python:3.9-slim
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY ./model\_artifact /app/model
COPY ./src /app/src
CMD ["uvicorn", "src.api:app", "--host", "0.0.0.0"]

4. Registros de Modelos

### Gobernanza de Artefactos

Control de versiones semánticas y estados de aprobación.

import mlflow
# Transicionar el modelo de Staging a Producción mediante API
client = mlflow.tracking.MlflowClient()
client.transition\_model\_version\_stage(
name="Fraud\_Detector",
version=4,
stage="Production",
archive\_existing\_versions=True
)

## Foco: Validación Cruzada (K-Fold)

### Definición y Concepto

La Validación Cruzada divide el dataset en **$K$ particiones**. Entrena $K$
veces, usando cada vez un grupo distinto para validar y el resto para entrenar,
promediando los resultados finales.

Uso en CI: Se utiliza en el pipeline para
asegurar que el performance del modelo no depende de un split aleatorio afortunado.

### Análisis Crítico

#### Ventajas

Estimación de error más realista. Aprovecha
todos los datos para validar. Reduce el sesgo de selección.

#### Desventajas

Coste computacional $K$ veces mayor. No apto
para series de tiempo (requiere TimeSeriesSplit).

## Higiene del Pipeline CI/CD

#### Error: Docker Bloat

Incluir el dataset de entrenamiento dentro de la imagen Docker. **Impacto:**
Imágenes de 10GB que tardan minutos en desplegar. **Solución:** Los datos se
consumen vía API o montaje de volúmenes; la imagen solo lleva código y pesos.

#### Práctica: Smoke Tests de Inferencia

Antes de desplegar, enviar 5 peticiones reales al contenedor en Staging. Si el modelo no
responde en < 100ms, el despliegue se cancela (Rollback).

### Phase Health Check

##### Build Success Rate

> 95%

##### MTTD

< 15 min

##### Rollback Rate

< 2%

## Checklist MLOps: CI/CD/CT

##### Docker Multi-stage Build

¿Se están usando imágenes optimizadas para reducir la superficie de
ataque y el tamaño?

##### Versiones Inmutables

¿Cada modelo en el Registry tiene un hash único vinculado al commit
de Git?

##### Métrica de Negocio en CI

¿El pipeline valida KPIs de negocio (ej. margen de ganancia esperado)
antes de aprobar?

##### Data Schema Validation

¿Se ejecuta un test de calidad de datos antes de iniciar el
re-entrenamiento (CT)?

##### Estrategia de Despliegue

¿Se ha definido el uso de Canary o Blue/Green para evitar impacto en
usuarios reales?

##### Rollback Automático

¿El orquestador es capaz de volver a la versión anterior si el
monitoreo detecta anomalías?

Industrial Standard v15.0 • MLOps CI/CD Automation • 2024

document.addEventListener('DOMContentLoaded', () => {
// Inicializar iconos
lucide.createIcons();
const cards = document.querySelectorAll('.topic-card');
cards.forEach(card => {
card.addEventListener('click', () => {
const index = card.dataset.index;
const color = card.dataset.color;
// Resetear todas las tarjetas
cards.forEach(c => {
const cColor = c.dataset.color;
// Clases inactivas
c.classList.remove('bg-white', 'shadow-xl', 'border-' + cColor + '-500', 'translate-x-2', 'active');
c.classList.add('bg-slate-50', 'border-slate-200');
// Ocultar detalles
const details = c.querySelector('.details-content');
details.classList.remove('visible');
details.classList.add('hidden');
});
// Activar tarjeta clickeada
card.classList.remove('bg-slate-50', 'border-slate-200');
card.classList.add('bg-white', 'shadow-xl', 'border-' + color + '-500', 'translate-x-2', 'active');
// Mostrar detalles
const details = card.querySelector('.details-content');
details.classList.remove('hidden');
details.classList.add('visible');
});
});
});